# Testnet Launch Checklist

## Current Gate

Run the readiness report at any time:

```bash
pnpm testnet:readiness:report
```

Re-run the chain ID registry check before any public announcement:

```bash
pnpm testnet:chain-id
```

The launch-blocking gate is intentionally strict:

```bash
TESTNET_READINESS_REQUIRE_SECRETS=1 pnpm testnet:readiness
PROJECT_ENV=testnet pnpm validate:config
```

Both commands must pass before a public Base Sepolia testnet is announced.

## Required Inputs

- [x] Candidate testnet chain ID selected: `895331` (`0xda963`).
- [x] Candidate testnet chain ID conflict-checked on 2026-05-05.
- [x] Base Sepolia parent RPC configured in `PARENT_RPC_URL`.
- [x] Base Sepolia parent WebSocket configured in `PARENT_WS_URL`.
- [x] Testnet deployer key stored as `TESTNET_DEPLOYER_PRIVATE_KEY`.
- [x] Testnet batcher key stored as `TESTNET_BATCHER_PRIVATE_KEY`.
- [x] Testnet proposer key stored as `TESTNET_PROPOSER_PRIVATE_KEY`.
- [x] Testnet unsafe block signer key stored as `TESTNET_SEQUENCER_PRIVATE_KEY`.
- [ ] Final launch chain ID rechecked with `pnpm testnet:chain-id` immediately before public announcement.

## Artifacts

- [x] `chain/configs/testnet/chain.json` has final chain ID, RPC, WebSocket, explorer, and status URLs.
- [x] `chain/configs/testnet/genesis.json` generated by op-deployer for the selected chain ID.
- [x] `chain/configs/testnet/rollup.json` generated by op-deployer for Base Sepolia parent chain ID `84532`.
- [x] `chain/configs/testnet/addresses.json` contains deployed parent and L3 bridge contracts.
- [x] `bridge/configs/testnet.bridge.json` matches the selected chain ID and Base Sepolia parent chain ID.
- [x] `pnpm testnet:export-artifacts` imported op-deployer outputs.
- [x] `bridge/superbridge/testnet/*` regenerated for testnet metadata.
- [x] `pnpm bridge:validate:testnet` passes.

## Preflight Tests

- [ ] `pnpm validate:config` passes in devnet mode.
- [ ] `pnpm test:e2e:sdk` passes on local devnet.
- [x] `PROJECT_ENV=testnet pnpm validate:config` passes with testnet artifacts.
- [x] `TESTNET_READINESS_REQUIRE_SECRETS=1 TESTNET_READINESS_LIVE=1 pnpm testnet:readiness` passes.
- [x] ETH deposit from Base Sepolia to Vellum succeeds.
- [x] L3 ETH transfer succeeds.
- [x] ERC-20 deployment, mint, and transfer succeed.
- [x] ETH withdrawal prove, dispute game resolve, and finalization succeed.

## Public Services

- [x] Public RPC online.
- [x] WebSocket RPC online.
- [x] Explorer online at `http://104.219.250.77:4000` with contract verification enabled.
- [x] Status service implementation available with RPC, explorer, bridge, faucet, and docs checks.
- [x] Faucet implementation available with enable flag, cooldowns, daily budget, and testnet-only guard.
- [x] Faucet online at `http://104.219.250.77:8788` with funding limits and a funded faucet wallet.
- [ ] Monitoring online.
- [x] Status page online at `http://104.219.250.77:3001`.
- [x] Docs published at `http://104.219.250.77:3003`.
- [ ] Reset policy documented.
- [ ] Known-risk banner published while reset policy is active.
